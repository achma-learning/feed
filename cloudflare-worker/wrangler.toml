name = "feed-aggregator"
main = "src/worker.js"
compatibility_date = "2024-12-01"

# Free plan is fine for this use case
# 100,000 requests/day, 10ms CPU time per invocation

[observability]
enabled = true
